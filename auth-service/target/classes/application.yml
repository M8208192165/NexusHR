server:
  port: 8081

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:postgresql://localhost:5432/nexushr
    username: postgres
    password: Mp@762003
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
    spring:
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
  cache:
    type: none
      

jwt:
  secret: ${JWT_SECRET:change-this-to-a-long-random-secret-in-prod}
  expiration-ms: 900000
  refresh-expiration-ms: 604800000