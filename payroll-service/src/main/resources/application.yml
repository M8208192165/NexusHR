server:
  port: 8083

spring:
  application:
    name: payroll-service
  datasource:
    url: jdbc:postgresql://localhost:5432/nexushr
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
